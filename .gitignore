# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtual env
anime-rec-env/
venv/
.venv/
env/

# Secrets / env (KHÔNG commit)
.env

# Runtime state (bộ nhớ JSON cũ — nay đã chuyển sang DB)
user_memory.json
*.db
*.sqlite3

# Dataset nặng không dùng cho web app (đã gỡ khỏi repo — xem CLAUDE.md)
data/manga_dataset.csv
# Model SVD sinh ra khi chạy train_svd.py (không commit, xem README)
data/svd_model.pkl

# Logs / OS
*.log
screenshot/server.log
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude Code local config (cá nhân)
.claude/settings.local.json
