flask==3.1.3
pandas==2.3.3
numpy>=1.26
scikit-learn==1.7.2
joblib>=1.3
gunicorn==23.0.0
Flask-SQLAlchemy>=3.1
SQLAlchemy>=2.0
Flask-Login>=0.6
psycopg2-binary>=2.9
python-dotenv>=1.0

# LƯU Ý: scikit-surprise KHÔNG có ở đây vì app web không cần nó.
# Nó chỉ dùng cho huấn luyện offline (train_svd.py) -> xem requirements-dev.txt
# (scikit-surprise phải biên dịch C++, sẽ làm hỏng build trên Render.)
